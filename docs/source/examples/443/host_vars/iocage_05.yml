properties:
  bpf: 1
  dhcp: 1
  vnet: 1
  notes: "vmm={{ inventory_hostname }}"

swarms:
  sw_01:
    count: 3
    plugin: ansible-zero

plugins:
  ansible-zero:
    git: https://github.com/vbotka/iocage-plugins
    options: bpf=1 dhcp=1 vnet=1
