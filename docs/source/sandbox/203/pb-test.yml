- name: Connect the swarm.
  hosts: swarm_sw_01
  gather_facts: false
  remote_user: admin
    
  tasks:

    - ansible.builtin.command: hostname
      register: out_host
    
    - ansible.builtin.debug:
        msg: >
          ansible_host={{ ansible_host }}
