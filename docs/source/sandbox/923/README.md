# Inventory plugin iocage2

The jail test-153 is used for testing. Start the jail if it is not running.

Edit the below files and quote dash in iocage_ip6 before running the
playbook pb-test-iocage-vars.yml

* /tmp/ansible/sandbox-923/iocage-test_153.yml
* /tmp/ansible/sandbox-923/iocage2-test_153.yml
