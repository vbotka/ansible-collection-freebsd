- name: Test empty iocage notes.
  hosts: iocage_05

  tasks:

    - command: iocage get notes test_5
      register: out

    - debug:
        msg: |
          out.stdout: {{ out.stdout }}
          out.stdout type: {{ out.stdout | type_debug }}
