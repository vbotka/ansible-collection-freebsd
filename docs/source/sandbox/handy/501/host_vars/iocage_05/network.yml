fn_gateway_enable: true
fn_defaultrouter: 10.10.0.1

fn_wlans:
  - {device: iwm0, interface: wlan0}

fn_cloned_interfaces: []
# - interface: bridge0

fn_interfaces:
  - {interface: wlan0, options: WPA SYNCDHCP, create: country SK regdomain ETSI}
# - {interface: bridge0, create: addm wlan1}
# - {interface: wlan1, options: WPA SYNCDHCP}
