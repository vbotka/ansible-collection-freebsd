- name: Connect to the group test.
  hosts: vmm_iocage_05
  gather_facts: false
  remote_user: admin
    
  tasks:

    - ansible.builtin.command: hostname
      register: out_host
    
    - ansible.builtin.debug:
        msg: |
          ansible_host: {{ ansible_host }}
          iocage_tags:
            {{ iocage_tags | to_nice_yaml }}
